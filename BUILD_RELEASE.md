# Building a Release

python build.py

# Quick test build with limited presets

# (Edit build_config.py and set PRESET_LIMIT = 2)

python build.py

# Use a custom config file

python build.py --config my_custom_config.py
